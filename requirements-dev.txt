flake8>=6.0.0
bandit[toml]>=1.7.9
pytest>=7.4.0
pytest-cov>=4.1.0

# starlette's TestClient needs httpx2; it falls back to httpx with a deprecation
# warning and raises if neither is installed. Nothing else pulls it in.
httpx2>=2.9.0
